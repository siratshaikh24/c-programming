#include<stdio.h>
int main()
{
   int n;
   printf("Enter 3 Numbers");
   scanf("%d%d%d",&a,&b,&c);
   if(a>b && a>c)
   Printf("max is %da");
   else if(b>c && b>a)
   printf("max is %db");
   else
   Printf("max is %d",c);
   return 0;
}